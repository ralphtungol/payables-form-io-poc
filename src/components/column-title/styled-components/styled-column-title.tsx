import styled from "styled-components";
import { theme } from "styled-tools";

export const StyledColumnTitleDiv = styled.div`
  font-weight: 900;
  font-size: 2.4rem;
  line-height: 3.2rem;
  padding-bottom: 2.8rem;
  color: ${theme("forgeLib.secondary.20")};
`;
